import { expect } from 'vitest';
import { config } from '@vue/test-utils';

// Aqui você pode adicionar qualquer configuração global para o Vue Test Utils

expect.extend({
  // Suas próprias matchers
});
